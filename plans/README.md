# Implementation Plans

Generated by the `improve` skill on 2026-06-17, against commit `515682b` (branch `main`).
Each plan is self-contained — an executor with no prior context can run it from the plan
file + the repo alone. Read the plan fully before starting, honor its STOP conditions, and
update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bind proxy + WS to loopback; reject cross-origin WS | P1 | S | — | DONE |
| 002 | Enable DNS-rebinding protection on the MCP server | P1 | S | — | DONE |
| 003 | Give the `reorder` batch op an error boundary | P2 | S | — | DONE (downgraded — see note) |
| 004 | Wire web **typecheck** into root scripts and CI (lint deferred → 010) | P1 | S | — | DONE |
| 005 | Test runner + characterization tests for the web theme engine | P2 | M | — | DONE |
| 006 | Harden project-root containment against symlink escape | P2 | M | — | DONE |
| 007 | Resolve the `ws` advisory + triage `pnpm audit` | P2 | S | — | DONE |
| 008 | Fix stale "not publishing" claim and package author | P3 | S | — | DONE |
| 009 | Investigate dynamic `className` silent-loss (spike) | P3 | M | — | DONE (found 3 gaps → 011) |
| 010 | Clean up web eslint errors + enable lint gate | P3 | M | 004 | TODO |
| 011 | Fail loud on unresolvable cn()/clsx() args (fix 009 gaps) | P3 | S | 009 | TODO |

## Implementation session — 2026-06-18

Plans 001–009 were implemented by dispatched executor subagents (one per plan,
isolated review per the `execute` flow), each diff reviewed and re-verified by
the advisor, then committed to branch `advisor/implement-plans` and merged to
`main`. Outcomes:

- **001, 002, 005, 006, 007, 008** — landed as specified; all done criteria
  re-verified (gates green).
- **003** — DOWNGRADED during execution. The premise (a reorder throw poisons
  the whole batch) did **not** reproduce: `executeBatch` Phase 4 already wraps
  `applyOp` in a per-op try/catch, and `mutateReorder` throws *before* mutating
  the AST. The change was kept as harmless defense-in-depth (every structural op
  self-contains its errors) plus a characterization test, but it is **not** a
  bug fix. (Advisor vetting miss — recorded honestly.)
- **004** — rescoped to **typecheck-only** in CI (web has 31 pre-existing lint
  errors); lint gate deferred to **010**.
- **009** — the spike **found 3 real silent-loss gaps** in `mutateClassName`
  (object / identifier / spread `cn()`·`clsx()` args fall through
  `checkConflictingConditional` to the `firstStr` append fallback with no error
  → silent duplicate or wrong-target edit). Characterization tests committed;
  fix tracked in **011**.

Follow-ups still open: **010** (web lint cleanup + gate) and **011** (fix the
className gaps 009 found).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended order & rationale

Do the **P1 security pair first** (001, 002) — highest leverage, small, clean verification.
001 is the single most important finding (proxy + file-writing WS exposed to the LAN).
Then the **cheap, low-risk wins**: 003 (one-op bug fix), 004 (CI gate — but run its Step 1
baseline first; it STOPs if the web app already has type/lint errors). Then the **larger
quality work**: 005 (web tests), 006 (symlink containment), 007 (dependency advisory).
Finally the **P3** items: 008 (docs/metadata) and 009 (a spike that may or may not produce a
follow-up plan).

## Dependency notes

- **2026-06-18 update:** plan 004 was scoped to **typecheck-only** in CI because the web app
  has 31 pre-existing eslint errors (some in the documented kibo intentional patch). The lint
  gate is deferred to plan **010**, which fixes the errors and extends 004's `check:web` to
  include lint. 010 depends on 004. Also note: the current `node_modules` was stale — `pnpm
  install` is required so `@vercel/analytics/next` resolves for the web typecheck.
- Aside from 004→010, no hard ordering dependencies between plans — they touch largely disjoint
  files and can be done in any order. The ordering above is by leverage/risk, not requirement.
- **Soft couplings to watch:**
  - **004 ↔ 005**: 004 wires web typecheck/lint into CI; 005 adds the web test runner. If both
    land, fold `pnpm --filter @themelab/web test` into 004's `check:web`/CI step (005's
    maintenance note covers this).
  - **001 ↔ 007**: both can touch the `ws` story. 001 edits the `WebSocketServer` construction;
    007 may bump `ws`. If 007 requires a `ws` **major** bump (API change), coordinate with 001
    (do 001 first, or re-run 001's tests after the bump).
  - **001 ↔ 002**: same security theme, different files (001 = proxy/WS in `index.ts`/`server.ts`;
    002 = MCP transport in `mcp/server.ts`). Independent; can be one PR or two.

## Findings considered and rejected (do not re-audit)

- **Remove "unused" `radix-ui` from `apps/web`** — NOT unused. It provides the kibo color
  picker's hue/alpha sliders (`Slider.Root/Track/Range/Thumb` in
  `apps/web/components/kibo-ui/color-picker/index.tsx`). Removing it breaks the picker.
- **Unhandled promise rejection in the `discoverFile` WS handler (`server.ts:710`)** — not a
  bug. `discoverFile` wraps its entire body in try/catch and returns `null`, so it never
  rejects; the missing `.catch` is harmless.
- **Kibo color-picker controlled-value RGB→HSL bug** — known/by-design. `apps/web/CLAUDE.md`
  documents that the controlled-value path is buggy and is worked around via `defaultValue` +
  a mount effect. Not worth fixing the unused path.
- **Color-picker "stale closure" footgun** — not a finding; the component is correct, this was
  parent-code hygiene with no concrete defect.
- **Remove `colorjs.io`** — entangled: it is used only by the vendored
  `lib/theme-engine/radix/generate-radix-colors.ts`, which is out of scope (verbatim Radix,
  `@ts-nocheck`). Can't remove without rewriting vendored code.
- **MCP `find_component` / `discoverFile` build a `RegExp` from input without escaping** — low
  severity (local, requires a connection; worst case is a thrown error or slow grep). Noted but
  not planned; could be a small hardening if 009/grep work touches this area.
- **File-size limit on theme writes, MCP auth token, config-perms-on-read, env-var-in-`ps`
  warnings** — low value under the local single-user trust model; the loopback bind (001) +
  DNS-rebinding protection (002) address the realistic attack surface.
- **God-file refactor of `batch-transform.ts` / `transform.ts` / `selection.ts`** — real
  tech debt (1500–1800 lines each, duplicated node-resolution paths) but L effort / MED-HIGH
  risk, and a poor fit for a low-context executor. Deferred deliberately: it should be preceded
  by characterization tests (003 and 009 begin building that net for the transform layer; the
  overlay would need its own before `selection.ts` is split). Revisit as a dedicated,
  test-first effort, not a handoff plan.

## Direction options (not planned — maintainer's call)

Surfaced during the audit, grounded in the repo; turn into design/spike plans only if chosen:

- **Theme mode v2** (ROADMAP M3, explicitly unshipped): fonts, shadows, letter-spacing,
  spacing, presets, import-from-pasted-CSS. The `ThemeStyles` model already carries the tokens;
  the overlay Theme panel doesn't edit them yet. Strongest "stated-but-undelivered" signal.
- **Studio → running-app round-trip**: one-click push of a theme generated in `apps/web` into
  the running project via the overlay/CLI. The `get_theme` MCP tool + `theme-writer.ts` make
  this cheap.
- **Ancestor breadcrumb navigation** (ROADMAP M1 marks it "still TODO").
- **Page-freeze / pin-hover during selection** (`tasks/todo.md` calls it the biggest remaining
  UX win; deferred).
